[project]
name = "bogeyboard"
version = "0.1.0"
description = "Garmin golf data scraper + dashboard"
requires-python = ">=3.12"
dependencies = [
    "garminconnect>=0.3.11",
    "polars>=1.0",
    "streamlit>=1.50",
]
